### Added

- **CNN Fear & Greed historical backfill via `whit3rabbit/fear-greed-data` mirror (#164).** New library `src/data_sources/sentiment_backfill.py` + thin script `scripts/backfill_fear_greed_whitrabbit.py` (pinned to upstream commit SHA `aa4f6009`) reads the community mirror CSV back to CNN's 2011-01-03 inception and merges into the existing `results/cnn_fg/YYYY.json` files via the same `_upsert(..., force=False)` semantics — backfill never clobbers a higher-fidelity CNN-direct entry. After the operator runs the one-shot script and commits the changed year files to the `data` branch, the demo's Long-Term Context tab renders ~14 years of monthly buckets instead of the ~13 months the live CNN endpoint alone covers. ADR-0005 amendment classifies whit3rabbit as a Tier-0 mirror; subindicators stay `null` for backfilled rows (per scope — only headline score + rating). License: upstream has no `LICENSE` file (tracked at `whit3rabbit/fear-greed-data#2`); accepted as-is on derivative-public-data basis. 9 new tests (TDD red → green).
