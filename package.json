{
  "name": "analyze-stock-kpi-demo",
  "private": true,
  "description": "Dev-only npm metadata for the ui/ dashboard. Hosts the Vitest unit harness, ESLint + Prettier JS tooling, and the TypeScript devDependency for JSDoc type-checking. The dashboard itself ships zero JS deps — Chart.js and Fuse.js are vendored under ui/vendor/.",
  "type": "module",
  "scripts": {
    "test": "vitest run tests/demo",
    "test:watch": "vitest tests/demo",
    "lint": "eslint ui tests/demo *.mjs",
    "lint:fix": "eslint ui tests/demo *.mjs --fix",
    "format": "prettier --write \"ui/**/*.js\" \"tests/demo/**/*.mjs\"",
    "format:check": "prettier --check \"ui/**/*.js\" \"tests/demo/**/*.mjs\""
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  }
}
